server:
  port: 8080
  servlet:
    context-path: /api

spring:
  application:
    name: neuro-sync
  datasource:
    url: jdbc:postgresql://localhost:5432/neurosync
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  # Security will be configured later
  security:
    user:
      name: admin
      password: admin
      roles: ADMIN

# Logging configuration
logging:
  level:
    org:
      springframework: INFO
      hibernate: INFO
    com.neurosync: DEBUG
  file:
    name: logs/application.log
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
